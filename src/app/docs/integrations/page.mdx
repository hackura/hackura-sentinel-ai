import { DocsPage } from '@/components/docs/docs-page';
import { GlassCard } from '@/components/ui';
import { INTEGRATION_ITEMS, INTEGRATIONS_TOC } from '@/content/docs/page-content';

<DocsPage
  eyebrow="Integrations"
  title="Connect intelligence providers into the pipeline"
  description="Hackura Sentinel AI is designed to enrich scans with trusted third-party sources and persist relationships where they matter most."
  toc={INTEGRATIONS_TOC}
>
  ## Integration stack

  <div className="grid gap-4 md:grid-cols-2 xl:grid-cols-3">
    {INTEGRATION_ITEMS.map((item) => (
      <GlassCard key={item.title} className="border-zinc-800/60 bg-zinc-950/60" hover={false}>
        <div className="mb-4 inline-flex rounded-2xl border border-purple-500/15 bg-purple-500/10 p-3 text-purple-300">
          {item.icon}
        </div>
        <div className="text-lg font-semibold text-white">{item.title}</div>
        <p className="mt-2 text-sm leading-7 text-zinc-400">{item.text}</p>
      </GlassCard>
    ))}
  </div>

  ## Supabase

  Supabase stores auth, onboarding state, and user profile context so the product can remember the workspace and reduce friction.

  ## OpenAI

  AI support can explain scoring and surface analyst summaries without replacing deterministic signals.
</DocsPage>
